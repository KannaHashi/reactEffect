import React from "react";
import ReactDOM from "react-dom";

// import LearningUseState from "./useState";
// import UseCallback from './useCallback'
import App from "./App";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <App kelas="XII RPL" />
  </React.StrictMode>,
  rootElement
);
